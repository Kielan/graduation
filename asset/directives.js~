'use strict';

var directives = angular.module('sampleApp.directives', [])

.directive('appDirective', function() {
  return {
    template: 'Name: {{developer.name}} Handle: {{developer.handle}}'
  };
});